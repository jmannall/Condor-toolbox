z = dlarray(2 + 3i);

r= real(z);
im = imag(z);

disp(['Complex: ' num2str(z)])
disp(['Real: ' num2str(r)])
disp(['Imaginary: ' num2str(im)])

%%

controlparameters = 
DefaultBTM(controlparameters);
gammaFiltBank = gammatoneFilterBank([50,20e3],'SampleRate',48e3);
%fvtool(gammaFiltBank)
[h,w] = freqz(gammaFiltBank);

h = mag2db(abs(h));

figure
semilogx(w, h)
xlim([20 20e3])