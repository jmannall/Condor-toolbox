####################
#
# Example Job for HTCondor
#
####################


#---------------------------------------------
# Name your batch so it's easy to distinguish in the q.
JobBatchName = "JM_BTMScript"

# --------------------------------------------
# Executable and its arguments
executable    = matlab-job.sh
arguments     = $(process)

# ---------------------------------------------------
# Universe (vanilla, docker)
universe         = vanilla

# -------------------------------------------------
# Event, out and error logs
log    = GenBtmData.p$(process).log
output = GenBtmData.p$(process).out
error  = GenBtmData.p$(process).error

# -----------------------------------
# File Transfer, Input, Output
should_transfer_files = YES
transfer_input_files = CondorScript.m
when_to_transfer_output = ON_EXIT_OR_EVICT

environment = "mount=$ENV(PWD)"
        
# --------------------------------------
# Resources
request_GPUs     = 1
+GPUMem          = 4000
request_CPUs     = 2
request_memory = 32G

#This job will complete in less than 1 hour
+JobRunTime = 8

#This job can checkpoint
+CanCheckpoint = true

# -----------------------------------
# Queue commands
queue 1
